<h1>Trump Timeline</h1>

Get the data in JSON format: <a href="/api/v1/events">/api/v1/events</a>

<h2>Table of Contents</h2>
<ul>
	{{#each events}}
	<li>
		<a href="#{{unique_id}}">{{start_date.year}} - {{text.headline}}</a>
	</li>
	{{/each}}
</ul>

<h2>Events</h2>
{{#each events}}
	<hr>
	<article id={{unique_id}}>
		<h3>{{start_date.year}} - {{text.headline}}</h3>
		<div>
			{{#if start_date.month}}Month: {{start_date.month}}{{/if}}
			{{#if start_date.day}}Day: {{start_date.day}}{{/if}}
		</div>
		<div>{{{text.text}}}</div>
		{{#if media.url}}
			<h4>Media</h4>
			{{{media.url}}}
			<br>
			{{media.caption}}
		{{/if}}
	</article>
	<br>
{{/each}}
